#temporals tables:
subject_test, subject_train: are tables with a people id who participated in experiment, including labels 

y_test, y_train: are tables with id activities, including labels

tb_values, tb_labels, and tb_subject : are tables with partial merge operations

# result tables
  
tidy_data: (table) is a complete tidy data set (point 1,3,4 complete)

tb_4 table: is a independent tidy data set with the average of each variable